{
  "tasks": {
    "test": "deno test --watch",
    "npm": "deno run --allow-env --allow-read --allow-write --allow-run --allow-net scripts/npm.ts",
    "version": "deno run --allow-read --allow-write scripts/version.ts",
    "local-coverage": "deno test --coverage=coverage && deno coverage --lcov --output=cov.lcov && deno coverage --html"
  },
  "lint": {
    "exclude": [
      "npm"
    ]
  },
  "fmt": {
    "exclude": [
      "npm",
      "CHANGELOG.md",
      ".github",
      ".vscode"
    ]
  },
  "test": {
    "exclude": [
      "CHANGELOG.md"
    ]
  },
  "name": "@levischuck/tiny-cbor",
  "version": "0.3.0",
  "license": "MIT",
  "exports": "./index.ts"
}
